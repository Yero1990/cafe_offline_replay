Order of items tracking study:

0) optimize track_criterion parameters (ptracking.param) --> DONE

1) study space point criterion (cluster size, will actually reduce number of track)

2) change size of scin geometry for pruning (and study pruning)

3) reduce drift time range (pdc_cuts.param, using p(h)min_drifttime  and p(h)max_drifttime )

4) matching the dc track to the scintillator plane (modify hcana)


** look at xptar,yptar,delta, chi2, ytar for (single and multi-track  events)
